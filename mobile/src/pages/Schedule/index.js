import React from 'react';
import { View, Text } from 'react-native';

import styles from './styles';

export default function Schedule() {
    return (
        <View style={styles.container}>
            <Text> Hello_Friend </Text>
        </View>
    );
};